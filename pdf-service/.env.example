# Port — Railway sets this automatically, do not override
PORT=3001

# Node environment
NODE_ENV=production

# Optional: restrict which origins can call this service
# Leave empty to allow the backend service only (use Railway private network)
ALLOWED_ORIGIN=https://cv-builder-api.railway.internal
